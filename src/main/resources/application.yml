server:
  port: 8081

spring:
  application:
    name: school
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/school?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false
    username: root
    password: 12345678
  config:
    import:
      - classpath:satoken-config.yml
mybatis-plus:
      configuration:
        map-underscore-to-camel-case: true
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      global-config:
        db-config:
          id-type: ASSIGN_ID
      mapper-locations: classpath:mapping/*.xml
      type-aliases-package: org.example.entity
satoken-config:
    whitelist:
      - /user/login
      - /user/logout
      - /error
      - /favicon.ico
    verifyKey: zzz